# PostgreSQL connection (replace values for your local database)
DB_URL=jdbc:postgresql://localhost:5432/ai_meeting_platform
DB_USERNAME=
DB_PASSWORD=

# Use a Base64-encoded secret of at least 32 bytes. Do not commit the real value.
JWT_SECRET=

# React development server origin
FRONTEND_ORIGIN=http://localhost:5173
